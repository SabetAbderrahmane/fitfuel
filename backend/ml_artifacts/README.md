# Local ML Artifacts

Copy trained FitFuel CV artifacts here for local backend inference:

- `binary_resnet50_best.pth`
- `binary_class_names.json`
- `food101_subset_resnet50_best.pth`
- `food101_subset_class_names.json`

Model weights and generated class-name files are intentionally ignored by Git.
